# GCP 연결 설정
GCP_PROJECT_ID=your-project-id
GCP_LOCATION=us
DOCUMENTAI_PROCESSOR_ID=your-processor-id
GOOGLE_APPLICATION_CREDENTIALS=./your-service-account-key.json

# Layout Parser 처리 옵션 (모두 기본값: 최고 품질)
RETURN_IMAGES=true
RETURN_BOUNDING_BOXES=true
CHUNK_SIZE=1024
INCLUDE_ANCESTOR_HEADINGS=true

# OCR 설정 (Layout Parser는 자체 OCR 내장 - false 유지)
# OCR 프로세서 사용 시 true로 변경
ENABLE_OCR_CONFIG=false
ENABLE_NATIVE_PDF_PARSING=true
ENABLE_SYMBOL=true
ENABLE_IMAGE_QUALITY_SCORES=true
COMPUTE_STYLE_INFO=true

# 프리미엄 기능 (OCR 2.0+ 프로세서에서만 사용 가능)
ENABLE_SELECTION_MARK_DETECTION=false
ENABLE_MATH_OCR=false
